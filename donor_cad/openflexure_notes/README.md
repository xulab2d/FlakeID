# OpenFlexure Notes

`OpenFlexure` was identified as a useful reference for:

- generic focus-drive concepts
- compact motor/coupler ideas
- low-cost motion-module layout

Direct repository clone failed in this environment, so no CAD was staged here.

That is not blocking the `Zeiss Axio` retrofit because the strongest current donor parts are already local from:

- `openUC2 / UC2-Motorized-XY-Table`
- `openUC2 / UC2-MicronStage`
- `PUMA`

If needed later, add OpenFlexure-derived donor geometry here with the same pattern:

- upstream README/license
- curated donor files
- manifest entry
